import cv2
import numpy as np


def correct_colors(img: np.ndarray) -> np.ndarray:
    """
    :param img: Image to correct colors for (not modified).
    :return: Image with corrected colors.
    """
    # TODO
    return img
